<div class="row mt-5 p-5 justify-content-center">
    <div class="col col-6">
        <div class="card p-3">
            <form>
                {{ form_start(form) }}
                <div class="mb-3">
                    <h3 class="text-center" >Indiquez le numéro de votre vol</h3>
                    {{ form_widget(form.flightNumber) }}
                </div>
                <div class="mb-3">
                    {{ form_widget(form.submit)}}
                </div>
                {{ form_end(form) }}
            </form>
        </div>
        
    </div>

</div>